# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/

# macOS
.DS_Store

# DMG artifacts
*.dmg

# Bundled FFmpeg binaries (downloaded via scripts/fetch-ffmpeg.sh)
WaxOnWaxOff/ffmpeg
WaxOnWaxOff/ffprobe

# Claude
.claude/

# Parity corpus (must live OUTSIDE the tree; backstop only)
/parity-corpus/
